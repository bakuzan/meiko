export declare enum PositionEnum {
  left = 'left',
  center = 'center',
  right = 'right'
}

export declare enum ViewOptionEnum {
  DUMMY_DAY = 1,
  DAY = 2,
  MONTH = 3
}

export declare enum EventCodes {
  ArrowDown = 'ArrowDown',
  ArrowLeft = 'ArrowLeft',
  ArrowRight = 'ArrowRight',
  ArrowUp = 'ArrowUp',
  Backspace = 'Backspace',
  Enter = 'Enter',
  Escape = 'Escape',
  Space = ' ',
  KeyQ = 'q'
}

export declare enum KeyCodes {
  q = 81,
  backspace = 8,
  enter = 13,
  escape = 27,
  up = 38,
  down = 40,
  space = 32
}

export declare const CLOSE_KEYS: (string | number)[];
export declare const OPEN_KEYS: (string | number)[];
export declare const ARROW_KEYS: string[];
