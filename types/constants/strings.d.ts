declare const Strings: {
  noItemsAvailable: string;
  save: string;
  cancel: string;
  monthNames: string[];
  dayNames: string[];
  // positions
  left: string;
  right: string;
  center: string;
  events: {
    click: string;
  };
};

export default Strings;
