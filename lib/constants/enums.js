export const PositionEnum = Object.freeze({
  left: 'left',
  center: 'center',
  right: 'right'
});

export const ViewOptionEnum = Object.freeze({
  DUMMY_DAY: 1,
  DAY: 2,
  MONTH: 3
});

export const KeyCodes = Object.freeze({
  q: 81,
  backspace: 8,
  enter: 13,
  escape: 27,
  up: 38,
  down: 40,
  space: 32
});

export const CLOSE_KEYS = [KeyCodes.space, KeyCodes.enter, KeyCodes.escape];
export const OPEN_KEYS = [KeyCodes.space, KeyCodes.enter];
