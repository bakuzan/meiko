import { sheet } from './nano';

export default sheet({
  formControl__error: {
    padding: '0 10px', // match control label padding
    marginTop: '-5px', // move error within control padding
    fontSize: '0.75em'
  }
});
